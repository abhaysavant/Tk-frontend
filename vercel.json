{
  "installCommand": "npm install --prefix \"tk bound\"",
  "buildCommand": "npm run build --prefix \"tk bound\"",
  "outputDirectory": "tk bound/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
